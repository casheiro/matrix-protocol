[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --shamefully-hoist --ignore-scripts"]

[phases.build]
cmds = ["pnpm build"]

[phases.start]
cmds = ["pnpm start"]
dependsOn = ["build"]